{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "okx-agentic-bot: autostart integrated",
      "type": "shell",
      "command": "npm",
      "args": ["run", "autostart:integrated"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "runOptions": {
        "runOn": "folderOpen"
      },
      "problemMatcher": []
    }
  ]
}
